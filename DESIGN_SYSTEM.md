# 🥋 سند جامع سیستم طراحی و هویت بصری بوشیدو (Bushido OS Design System)
> **نسخه:** ۲.۰ (Single Source of Truth & Color Benchmark Edition)  
> **مرجع فنی و طراحی برند:** Bushido Discipline OS  
> **هدف:** تدوین استانداردهای واحد بر اساس بنچ‌مارک‌های بین‌المللی کنتراست ادراکی (W3C APCA و WCAG 2.2)، الگوهای تعاملی (Affordance)، مقیاس‌های هندسی، پالت مشکی آبسیدین (Obsidian Black) و رفتار ریسپانسیو برای توسعه پایدار و یکپارچه در تمام پلتفرم‌های برند بوشیدو.

---

## ۱. بنچ‌مارک ارزیابی رنگ‌ها و سیستم امتیازدهی کنتراست (Color Benchmark & Scoring)

در طراحی رابط‌های کاربری مدرن و سیستم‌های دارک‌مود حرفه‌ای، بنچ‌مارک مرجع بین‌المللی **W3C APCA (Advanced Perceptual Contrast Algorithm)** به همراه **WCAG 2.2 AAA** و مدل رنگی ادراکی **OKLCH** است.

### الف) الگوریتم APCA و نحوه امتیازدهی آن چیست؟
الگوریتم APCA به جای فرمول سنتی و خطی کنتراست (که در محیط‌های تیره خطای ادراکی بالایی داشت)، کنتراست ادراکی چشم انسان ($L_c$) را بر اساس ۴ فاکتور محاسبه می‌کند:
1. **گشادی مردمک چشم در دارک‌مود (Pupil Dilation & Glare):** جلوگیری از پدیده هاله نوری (Halation) و خیرگی متن‌های خیلی روشن روی پس‌زمینه‌های تیره.
2. **فرکانس فضایی و وزن فونت (Spatial Frequency & Font Weight):** متن‌های ریز به کنتراست بالاتری نسبت به سرتیترهای بولد نیاز دارند.
3. **اشباع و آندرتون پس‌زمینه (Chroma & Undertone):** پس‌زمینه‌های با اشباع آبی بالا (مثل سرمه‌ای خالص) باعث خستگی سریع شبکیه چشم می‌شوند؛ در حالی که مشکی خالص ابریشمی/آبسیدین با اشباع زیر ۳٪، حداکثر راحتی بصری و کنتراست ارگونومیک را ایجاد می‌کند.
4. **کفایت سطح کنتراست ($L_c \ge 60$ برای متن بدنه و $L_c \ge 75$ برای ارقام و نشان‌ها).**

---

### ب) کارنامه ارزیابی و امتیاز بوشیدو در بنچ‌مارک بین‌المللی (Scorecard)

| نام عنصر / شاخص بصری | ترکیب رنگی (پیش‌زمینه / پس‌زمینه) | نسبت کنتراست WCAG | امتیاز APCA ($L_c$) | وضعیت استاندارد | تحلیل ارگونومی |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **متن اصلی بدنه** | `#f4f4f5` روی `#09090b` (Obsidian) | **۱۶.۲ : ۱** | **$L_c = 104$** | **گرید پلاتینیوم (AAA+)** | وضوح مطلق بدون خیرگی به دلیل استفاده از آف‌وایت زینک |
| **متن ثانویه و توضیحات** | `#a1a1aa` روی `#121215` (Card) | **۷.۴ : ۱** | **$L_c = 78$** | **گرید طلایی (AAA)** | خوانایی بی‌نقص برای پاراگراف‌های طولانی و زیرنویس‌ها |
| **طلای بوشیدو (Samurai Amber)** | `#fbbf24` روی `#09090b` / محفظه `amber-500/10` | **۱۲.۴ : ۱** | **$L_c = 88$** | **گرید پلاتینیوم (AAA)** | حداکثر درخشش ادراکی برای اکشن‌های VIP و امتیاز کل |
| **سبز فتح (Vitality Emerald)** | `#34d399` روی `#09090b` / محفظه `emerald-500/10` | **۱۱.۲ : ۱** | **$L_c = 82$** | **گرید پلاتینیوم (AAA)** | کنتراست زلال و آرام‌بخش برای روزهای استاندارد ۵/۵ |
| **سرخ آتشین زنجیره (Fiery Rose)** | `#fb7185` روی `#09090b` / محفظه `rose-500/10` | **۸.۶ : ۱** | **$L_c = 73$** | **گرید طلایی (AAA)** | گرمای بصری شعله بدون تداخل با سیگنال‌های هشدار |
| **سرخ بحران و بدهی (Alert Red)** | `#f87171` روی کادر `#121215` و بردر اخطار | **۷.۸ : ۱** | **$L_c = 70$** | **گرید طلایی (AAA)** | هشدار صریح و خوانا برای تسویه بدهی و قفل دیسیپلین |
| **بنفش کالبدشکافی (Violet)** | `#c084fc` روی `#09090b` / محفظه `purple-500/10` | **۹.۴ : ۱** | **$L_c = 76$** | **گرید طلایی (AAA)** | تفکیک داده‌های تاریخی بسته‌شده از وضعیت‌های باز |

---

## ۲. فلسفه طراحی و مهاجرت به مشکی آبسیدین (The Obsidian Black Revolution)

### چرا سرمه‌ای (Slate) کنار گذاشته شد و مشکی آبسیدین انتخاب شد؟
1. **هویت اصیل سامورایی (True Bushido Armor):** رنگ‌های اصلی نمادین بوشیدو **مشکی مات زره سامورایی، زرشکی عمیق، طلای خرد و سپید پولادین** است. تم‌های سرمه‌ای/آبی با فلسفه بوشیدو همخوانی تاریخی و برندینگ نداشتند.
2. **حذف خستگی چشم و نور آبی مضر در شب:** پس‌زمینه `slate-950` دارای ته رنگ آبی تیره بود. پالت **Obsidian Black / Tactical Zinc Charcoal (`#09090b`)** اشباع رنگی را به زیر ۲٪ می‌رساند که چشم‌نوازترین بستر دارک‌مود برای نبردهای شبانه است.

### مقیاس پالت خنثی آبسیدین (The Obsidian Neutral Scale):
- **بستر اصلی سراسری (App Canvas Root):** `#09090b` (`bg-[#09090b]` یا `zinc-950`)
- **کانتینرها و کارت‌های سطح اول (Elevated Cards):** `#121215` / `#141418` (`bg-[#121215]` یا `zinc-900/90`)
- **کادرها و خطوط مرزی (Subtle Borders):** `#27272a` (`border-zinc-800` با شفافیت دقیق)
- **جداکننده‌ها و هاور کادرها (Muted Dividers & Hover Borders):** `#3f3f46` (`border-zinc-700`)
- **تایپوگرافی اصلی (Heading & Numbers):** `#f4f4f5` (`text-zinc-100` / `text-white`)
- **تایپوگرافی توضیحات و بدنه (Body & Subtitles):** `#a1a1aa` (`text-zinc-400`)
- **تایپوگرافی ارقام شبح و لیبل‌های فرعی (Muted Captions):** `#71717a` (`text-zinc-500`)

---

## ۳. پالت رنگ، توکن‌های معنایی و استانداردهای پاپ‌آپ‌ها و کادرها

### الف) جدول توکن‌های رنگی معنایی:

| توکن رنگ | کد هگز / کلاس Tailwind | نقش در سیستم | شدت و بنچ‌مارک در دارک‌مود | حالت تعاملی / هاور |
| :--- | :--- | :--- | :--- | :--- |
| **Samurai Amber** (طلای بوشیدو) | `#fbbf24` (`amber-400` / `amber-500`) | اکشن‌های اصلی، هوش مصنوعی، امتیاز کل، ارتقا VIP، صدور حکم دادگاه | متن `amber-400`، بکگراند `amber-500/10`، بردر `amber-500/20` | `hover:bg-amber-400 active:bg-amber-600` |
| **Vitality Emerald** (سبز فتح) | `#34d399` (`emerald-400` / `emerald-500`) | روز استاندارد (۵/۵)، چک‌باکس‌های تایید شده، پایداری عادات | متن `emerald-400`، بکگراند `emerald-500/10`، بردر `emerald-500/20` | `hover:bg-emerald-400 text-emerald-300` |
| **Fiery Rose** (سرخ آتشین زنجیره) | `#fb7185` (`rose-400` / `rose-500`) | زنجیره فعال (Pure Streak)، طولانی‌ترین زنجیره تاریخ | متن `rose-400`، بکگراند `rose-500/10`، بردر `rose-500/20` | `border-rose-500/30 text-rose-300` |
| **Honor Crimson** (سرخ کالبدشکافی و بدهی) | `#f87171` (`red-400` / `red-500`) | بدهی‌های باز، قفل دیسیپلین، هشدارهای عدم تعهد، روزهای سوخته | متن `red-400`، بکگراند `red-950/40`، بردر `red-500/50` با پالس اخطار | `hover:bg-red-900/80 border-red-500/60` |
| **Frost Blue** (آبی فریز) | `#60a5fa` (`blue-400` / `blue-500`) | توقف اضطراری موجه (دلایل شخصی و فریز روز) | متن `blue-400`، بکگراند `blue-500/10`، بردر `blue-500/20` | `border-blue-500/40 bg-blue-950/70` |
| **Bushido Violet** (بنفش تحلیل) | `#c084fc` (`purple-400` / `purple-500`) | بدهی کالبدشکافی شده (پرونده بسته)، اصلاحات تثبیت‌شده | متن `purple-400`، بکگراند `purple-500/10`، بردر `purple-500/20` | `border-purple-500/30 text-purple-300` |

---

### ب) قانون استقلال زنجیره از تم انتخابی (Universal Pure Streak Law):
- **معضل قبلی:** اگر کاربر تم سرخ را انتخاب می‌کرد، نشان زنجیره در هدر سرخ زرشکی می‌شد ولی در اتاق فرماندهی سرخ آتشین (`rose-400`) بود. همچنین اگر تم سبز انتخاب می‌شد، زنجیره سبز می‌شد که معنای شعله و آتش را مخدوش می‌کرد.
- **قانون قطعی سیستم:** نشان زنجیره خالص (`Pure Streak`) با آیکون `Flame` یک ستون هویتی و غیرقابل تغییر است. در تمام بخش‌های سیستم (هدر بالا، اتاق فرماندهی، تالار رکوردها و بایگانی)، رنگ زنجیره **منحصراً و دائماً Fiery Rose (`text-rose-400 bg-rose-500/10 border-rose-500/20`)** است و انتخاب تم رابط کاربری (توسط کاربر) صرفاً دکمه‌های ناوبری، تب‌های فعال و هایلایت‌های فرعی را تغییر می‌دهد.

---

### ج) اصول رنگ‌بندی در کادرهای بایگانی، پاپ‌آپ‌ها و مودال‌ها:

1. **قانون تفکیک کادر و متن (Container vs Body Text Contrast):**
   - **کادرها و بنرها (مانند نقاط قوت، نقاط ضعف، کالبدشکافی):**
     - پس‌زمینه کادر: با شفافیت بسیار ملایم ۸ تا ۱۰ درصد (`bg-emerald-500/10` یا `bg-amber-500/10`).
     - بردر کادر: شفافیت ۲۰ تا ۳۰ درصد (`border-emerald-500/20`).
     - سرتیتر و آیکون بخش: رنگ معنایی شاخص (`text-emerald-400` یا `text-amber-400`).
     - **متن بدنه و توضیحات داخل کادر:** اکیداً رنگ خنثی خوانا (`text-zinc-300` یا `text-zinc-400`). هرگز نباید یک پاراگراف کامل را با رنگ سبز یا قرمز پررنگ نوشت؛ این کار باعث خطای ارگونومیک و خستگی چشم می‌شود.
2. **پاپ‌آپ‌ها و مودال‌ها (دادگاه هوش مصنوعی، کالبدشکافی، ارتقا VIP):**
   - پس‌زمینه مودال: مشکی آبسیدین با سایه عمیق (`bg-[#121215] border border-zinc-800`).
   - متون توضیحی و سوالات: رنگ خنثی با کنتراست بالا (`text-zinc-100` برای عنوان و `text-zinc-400` برای سوالات).
   - متون رنگی در پاپ‌آپ‌ها **فقط و فقط مجاز برای کلمات کلیدی، نشان‌ها و مقادیر عددی** است (مانند بج `+۱۰ امتیاز` به رنگ کهربایی یا برچسب `بدهی ثبت شد` به رنگ سرخ اخطار).

---

---

### د) یکپارچگی رنگ برند اصیل بوشیدو و قوانین وضعیت‌ها (Unified Bushido Crimson & Status Architecture):
1. **رنگ برند واحد (Single Brand Core - Bushido Crimson `#E11D48`):**
   - به منظور جلوگیری از پراکندگی هویت بصری و تثبیت اقتدار برند بوشیدو، سیستم به صورت ۱۰۰٪ روی رنگ سرخ اصیل بوشیدو (`#E11D48` / `rose-500`) قفل و متمرکز است.
   - بخش انتخاب تم از تنظیمات کاربر حذف شده تا سیستم دارای هویتی یکدست، باوقار و اصیل باشد.
2. **قانون نشانگر وضعیت چرخه (Cycle Status Badge Rule):**
   - نشانگرهای وضعیت با متون رسا و ساختار کادری دقیق بدون نیاز به نقاط رنگی اضافه (مثل توپ طلایی) طراحی می‌شوند:
   - محفظه تیره آبسیدین با کادر باریک روبی برند (`bg-[#09090b]/80 border border-rose-500/30 text-rose-200`)، وضوح و تمایز کافی ایجاد می‌کند بدون اینکه ایجاد نویز بصری کند.
3. **قانون نمایش شاخص انضباط (Discipline Score Presentation):**
   - عدد اصلی شاخص انضباط به صورت پلاتینیوم با کنتراست فوق‌العاده بالا (`text-zinc-100 font-mono font-black`) رندر می‌شود تا خستگی چشم ایجاد نکند.
   - وضعیت عملکردی آن با برچسب معنایی تفکیک می‌شود: سبز زمردی (`emerald-500/10`) برای عملکرد عالی (بالای ۸۰٪) و سرخ اخطار برای وضعیت بحرانی (زیر ۷۰٪).
4. **رنگ لهجه در نوار ناوبری (Bottom Nav & Header):**
   - رنگ سرخ اصیل بوشیدو (`#E11D48`) به عنوان نقطه فوکوس (Brand Accent) در تب فعال نوار ناوبری پایین کاملاً استاندارد و منطبق بر بنچ‌مارک‌های مطرح دنیا (مانند Netflix و Apple Fitness) عمل می‌کند.
   - هشدارهای خطای واقعی (بدهی باز / سوختگی روز) منحصراً با توکن اخطار پالس‌دار (`animate-ping / animate-pulse bg-red-500`) و آیکون هشدار همراه هستند.
5. **قانون سطح کادرها و کارت‌ها (Sub-Surface Obsidian Depth & Ruby Ambient Tint):**
   - پس‌زمینه کادرها بر پایه مشکی آبسیدین با ۲٪ ته‌رنگ کروماتیک روبی لوکس (`bg-[#131215]` / `.bg-card-obsidian`) و محفظه‌های داخلی (`bg-[#0c0b0e]` / `.bg-inner-obsidian`) پیاده‌سازی شده است.
   - این ته‌رنگ بدون کاهش کنتراست ارگونومیک و خوانایی متن‌های بدنه ($L_c \ge 78$)، هویت اصیل برند بوشیدو را در لایه زیرین کادرها و بردرهای تعاملی هوور (`card-hover-ruby`) زنده و متمایز نگاه می‌دارد.

---

### الف) قوانین عناصر تعاملی و دکمه‌ها (Buttons & Actions):
* **نشانگر ماوس:** اجباراً `cursor-pointer`.
* **انیمیشن لمسی/کلیک:** دارای فیدبک بصری فوری (`transition-all active:scale-[0.98]`).
* **متن و برچسب:** متن دکمه‌ها **همواره یک‌خطی** است و کلاس `whitespace-nowrap` الزامی است.
* **ارتفاع‌های استاندارد دکمه:**
  - دکمه‌های هدر و نوار بالا: فیکس `h-8 sm:h-9`
  - دکمه‌های تقویم و اکشن‌های بار: فیکس `h-10`
  - دکمه‌های کارت‌های روزانه (Habit Cards): پدینگ `p-3.5 sm:p-4 rounded-2xl`
* **بازخورد صوتی:** هر کلیک روی دکمه‌های تعاملی با افکت صوتی اختصاصی WebAudio همراه است.

### ب) قوانین بج‌ها و تگ‌های اطلاعاتی (Status Chips & Badges):
* **نشانگر ماوس:** اجباراً `cursor-default select-none pointer-events-none`.
* **سبک بصری:** پس‌زمینه مات/تیره نیمه‌شفاف با بردر نازک ۱ پیکسلی همرنگ وضعیت، به شکل کپسول (`rounded-full`) یا تگ کوچک (`rounded-lg`).
* **بدون سایه دکمه‌ای یا افکت هاور:** هیچ انیمیشن جابه‌جایی یا هاور رنگی نباید روی بج‌های اطلاعاتی اعمال شود تا کاربر تصور نکند کلیک‌پذیر هستند.

### ج) قوانین کارت‌های نمایشی (Information Cards):
* کادرهای آمار، تالار رکوردها و باکس‌های متنی صرفاً نقش سازمان‌دهی اطلاعات را دارند و نشانگر ماوس در آنها `cursor-default` است.

---

## ۴. مقیاس هندسی آیکون‌ها و کانتینرها (Icon Sizing Hierarchy)

برای حفظ هارمونی بصری، سایز آیکون‌ها و محفظه‌های دور آنها در ۵ سطح مهندسی شده است:

| سطح (Level) | سایز محفظه (Box) | سایز آیکون (Icon) | انحنا (Radius) | موارد استفاده |
| :--- | :--- | :--- | :--- | :--- |
| **Level 1 (Hero Master)** | `w-12 h-12` (48px) | `w-6 h-6` (24px) | `rounded-2xl` | هدر اصلی صفحات، آواتار پروفایل، لوگوی چرخه |
| **Level 2 (Section Header)** | `w-10 h-10` (40px) | `w-5 h-5` (20px) | `rounded-xl` | سرتیتر تالار رکوردها، پیام مربی بوشیدو، هدر بخش‌ها |
| **Level 3 (Interactive Cards)**| `w-10 h-10 shrink-0` (40px)| `w-5 h-5` (20px) | `rounded-xl` | کارت‌های ۵گانه فونداسیون (سحرخیزی، ورزش...) و ماموریت ویژه |
| **Level 4 (Stat & Hall Items)** | `w-8 h-8 shrink-0` (32px) | `w-4 h-4` (16px) | `rounded-xl` | کارت‌های تالار رکوردها، تگ‌های آمار فرعی |
| **Level 5 (Micro & Badges)** | `w-6 h-6` یا اینلاین | `w-3.5 h-3.5` (14px) | `rounded-lg` | چک‌باکس‌های مینی جدول آرشیو، آیکون‌های درون دکمه‌ها |

---

## ۵. قوانین تایپوگرافی، ترازبندی و ضدشکست متن (Typography & Spacing)

1. **ارقام و اعداد:**
   - کلیه اعداد و درصدها در تمامی کامپوننت‌ها باید توسط تابع `toPersianDigits(num)` به ارقام فارسی تبدیل شوند تا یکدستی زبان حفظ گردد.
   - اعداد آماری بزرگ و تایمرها از کلاس `font-mono` برای خوانایی مکانیکی استفاده می‌کنند.

2. **قانون تفکیک شکست خط (Wrapping vs Truncation):**
   - **دکمه‌ها، بج‌ها و تگ‌ها:** اکیداً تک‌خطی و ضد شکست با کلاس `whitespace-nowrap`.
   - **عناوین اصلی (Titles):** تک‌خطی با `truncate` در صورت کمبود فضا.
   - **متون توضیحی و راهنماها (Descriptions):** هرگز از `truncate` با سه نقطه (...) استفاده نشود؛ بلکه باید به صورت چندخطی طبیعی با `leading-relaxed text-right` زیر هم قرار گیرند تا پیام به طور کامل و بدون بریدگی خوانده شود.

3. **چیدمان درون کارت‌های روزانه (مانند سحرخیزی و ماموریت ویژه):**
   - عنوان فارسی + عنوان انگلیسی در خط اول با کلاس `leading-snug flex items-center gap-1.5`.
   - زیرعنوان توضیحی در خط دوم با کلاس `leading-relaxed text-xs text-zinc-400 text-right`.
   - محفظه متن باید `min-w-0 flex-1 space-y-1` باشد تا نسبت به آیکون کناری و دکمه تیک در مرکز عمودی بی‌نقص قرار گیرد.

4. **معماری یکپارچه سرتیتر بخش‌ها (Section Header Anatomy):**
   - سرتیترهای بخش‌ها (مانند «۵ رکن فونداسیون» و «ماموریت شتاب‌دهنده») دارای ساختار متقارن دوخطی هستند:
     - **خط اول:** عنوان بخش همراه با آیکون سرتیتر (`font-bold text-sm text-zinc-200 flex items-center gap-2`).
     - **خط دوم:** متن توضیحی کوتاه راهنمای بخش (`text-xs text-zinc-400`).

5. **استاندارد نام‌گذاری و نوار ناوبری تقویم (Calendar & Temporal Nomenclature):**
   - **روز جاری:** صرفاً با عنوان رسمی `روز جاری نبرد` (بدون تکرار کلمه امروز و بدون پرانتزهای اضافی).
   - **روزهای آینده:** به فرمت دقیق و مینیمال `${n} روز بعد`.
   - **روزهای گذشته:** به فرمت دقیق و مینیمال `${n} روز قبل`.
   - **دکمه‌های روز قبل و بعد:** با ابعاد متقارن `h-10 px-3.5` و کلاس‌های `inline-flex items-center justify-center gap-1.5 whitespace-nowrap leading-none` برای تراز عمودی و ریاضی بی‌نقص.
   - **کادر کات‌آف شبانه:** با ارتفاع هم‌تراز `h-10` و پدینگ `px-3.5 rounded-xl border border-zinc-800` که در نمای موبایل به صورت وسط‌چین (`justify-center`) و در دسکتاپ به صورت تراز انتهای نوار (`sm:justify-end`) چیدمان می‌شود.
   - **کارت وضعیت روزهای آینده (Future Lock Banner):** با الهام از Untitled UI به صورت کامپوننت متوازن خنثی (`bg-[#121215]/80 border-zinc-800`) همراه با آیکون قطب‌نمای آینده (`Compass`) و دکمه بازگشت با هایلایت امبر مینیمال (`bg-amber-500/10 hover:bg-amber-500/20 text-amber-300 border-amber-500/30`) هم‌تراز با دکمه پرش به امروز در نوار تقویم طراحی می‌شود.

6. **مهندسی کانتینر فرضی متن در کارت‌های عادات (Habit Card Text Box Anatomy):**
   - کارت‌های ۵گانه و شتاب‌دهنده از یک ساختار سه‌بخشی متوازن پیروی می‌کنند:
     - **بخش راست (آیکون):** محفظه سطح ۳ با ابعاد فیکس `w-10 h-10 rounded-xl shrink-0 flex items-center justify-center`.
     - **بخش میانی (کانتینر فرضی متن):** با کلاس‌های `min-w-0 flex-1 flex flex-col justify-center space-y-1` تا متن چه ۱ خط باشد و چه ۲ خط، همواره در مرکز عمودی تراز آیکون و دکمه چک‌باکس باقی بماند.
     - **بخش چپ (دکمه اکشن چک‌باکس):** ابعاد فیکس `w-7 h-7 rounded-xl shrink-0 flex items-center justify-center`.

7. **تمایز آیکون‌های سرتیتر و اکشن‌ها (Icon Semantic Distinctiveness):**
   - برای جلوگیری از تکرار بصری آیکون‌ها، در سرتیتر ماموریت شتاب‌دهنده از آیکون شتاب و پرواز (`Rocket`) و در داخل کارت از آیکون هدف و شکار (`Target`) استفاده می‌شود.
   - در سرتیتر ۵ رکن فونداسیون، آیکون شمشیرهای تعهد (`Swords`) با رنگ زمردین (`emerald-400`) به عنوان شرط اساسی روز استاندارد تعبیه شده است.

8. **استاندارد جعبه یادداشت‌ها و ذخیره‌سازی خودکار (Auto-Expanding Notes Architecture):**
   - فیلد یادداشت‌های روزانه به صورت فرم خودکار (`Auto-Expanding Textarea`) بدون دستگیره کشش دستی نامتعارف طراحی می‌شود تا با افزایش تعداد خطوط، فیلد به صورت طبیعی و بدون ایجاد اختلال در اسکرول صفحه رشد کند.
   - وضعیت ذخیره‌سازی با مکانیزم Debounce خودکار با نشانگر وضعیت (`ذخیره شد ✓` / `در حال ذخیره...`) به کاربر اطمینان آنی می‌دهد.
   - در روزهای آینده یا چرخه‌های بایگانی‌شده، وضعیت غیرفعال با طراحی کدر (`opacity-60 cursor-not-allowed`) و پیام شفاف همراه است.

9. **استاندارد معماری اتاق فرماندهی (Cycle Command Center Standards):**
   - **هیرو وضعیت چرخه:** حذف اضافه‌گویی و تکرار اطلاعات؛ نمایش صریح `روز X از ۹۰` بدون پرانتزهای زائد معکوس، به همراه نوار پیشرفت صیقلی (`Progress Bar`) با درصد سپری‌شده، بنر پیام مربی با آیکون نقره‌ای سطح ۲ (`Zap`) و ستون شاخص انضباط (`Discipline Score`) با تایپوگرافی مونو و مخرج شبح.
   - **تفکیک و وقار رنگی آیکون‌های سرتیتر:** برای جلوگیری از اشباع غیرحرفه‌ای رنگ طلایی در صفحه، تمام سرتیترهای بخش‌های تحلیلی (تقویم ماتریس، آسیب‌پذیری بحرانی، الگوهای اصطکاک و بنر مربی) از آیکون‌های خنثی نقره‌ای (`text-zinc-300`) استفاده می‌کنند و رنگ طلایی فقط مختص نشان‌های قهرمانی و افتخار (`Trophy` و امتیاز کل) و رتبه بوشیدو باقی می‌ماند.
   - **تالار رکوردها و معیارهای چرخه (Hall of Records & Bento Grid Standards):**
     - **یکپارچگی مطلق آیکون‌ها:** در هر دو بخش «معیارهای چرخه فعلی» و «تالار رکوردهای تاریخ»، آیکون‌های متناظر کاملاً یکسان هستند:
       - زنجیره متوالی (Streak): الزماً آیکون `Flame` در پس‌زمینه `rose-500/10` با رنگ `rose-400`.
       - روزهای استاندارد (Standard Days): الزماً آیکون `CheckCircle2` در پس‌زمینه `emerald-500/10` با رنگ `emerald-400`.
       - امتیاز کل (Total Score): الزماً آیکون `Award` در پس‌زمینه `amber-500/10` با رنگ `amber-400`.
       - بالاترین امتیاز کل (Total Score): الزماً آیکون `Award` در پس‌زمینه `amber-500/10` با رنگ `amber-400`.
     - **مقایسه شفاف با چرخه جاری:** در هر کارت رکورد، زیرنویس مقایسه‌ای «در چرخه فعلی: X» با فونت مونو و رنگ اختصاصی همان شاخص طراحی شده تا کاربر در یک نگاه عملکرد دوره فعلی را نسبت به سقف تاریخی خود بسنجد.
   - **ماتریس تعاملی ۹۰ روزه (90-Day Matrix):** نمایش شبکه متقارن روزها با رنگ‌بندی داینامیک، هایلایت روز جاری با رینگ طلایی (`ring-2 ring-amber-400 scale-105`) و ناوبری مستقیم با یک کلیک به صفحه میدان نبرد همان روز.
   - **تحلیل اصطکاک و دکمه‌های اکشن:** کارت‌های ریشه‌یابی و آسیب‌پذیری بحرانی همراه با دکمه‌های پرش به میدان نبرد امروز و مشاهده بایگانی با افکت فشرده‌سازی `active:scale-[0.98]`.

---

## ۶. اصول ریسپانسیو و تجربه موبایل (Mobile UX Standards)

1. **استانداردسازی یکپارچه هدر و نوار بالا (Top Hub Bar Architecture):**
   - تمام عناصر کنترلی در هدر بالا اعم از نشان برند، دکمه انتخاب چرخه، بج زنجیره خالص و آیکون کاربری از ارتفاع یکپارچه هندسی پیروی می‌کنند:
     - **در موبایل (< 640px):** ارتفاع فیکس `h-8` (۳۲ پیکسل)
     - **در تبلت و دسکتاپ (≥ 640px):** ارتفاع فیکس `h-9` (۳۶ پیکسل)
   - **تفکیک ارگونومیک قطب‌های هدر در چیدمان راست‌به‌چپ (RTL):**
     - **سمت راست (شروع مطالعه):** هویت و زمینه با نشان اختصاری `武` و منوی بازشوی انتخاب چرخه ۹۰ روزه.
     - **سمت چپ (مرکز وضعیت و تمرکز):** بج زنجیره خالص متوالی (`🔥 X روز`)، هشدار بدهی‌های باز (در صورت وجود) و بج وضعیت VIP (در صورت فعال بودن). دکمه ارتقای اشتراک طبق قانون هیک (Hick's Law) از فضای پرتنش هدر حذف و به تب حساب/پروفایل و نقاط عطف هوشمند (Milestone Triggers) منتقل شده است.
2. **ناوبری دوگانه هوشمند (Desktop Navbar vs. Mobile Bottom Nav):**
   - در دسکتاپ (`lg` به بالا): تب‌های افقی در هدر بالا قرار دارند.
   - در موبایل (`< lg`): یک نوار ثابت و ارگونومیک در پایین صفحه (`bottom-0 pb-safe`) با ۴ تب اصلی قرار می‌گیرد تا انگشت شست به راحتی به تمام بخش‌ها دسترسی داشته باشد.
3. **سقف عرضی محتوا (Container Bounds):**
   - حداکثر عرض در دسکتاپ بر روی `max-w-5xl` یا `max-w-6xl` محدود می‌شود تا محتوا روی مانیتورهای عریض کشیده نشود.
4. **تطبیق جداول به کارت در موبایل:**
   - جدول‌های سنگین (مانند سوابق ۹۰ روزه) روی موبایل به صورت کارت‌های تجمیعی عمودی نمایش داده می‌شوند تا نیازی به اسکرول افقی آزاردهنده نباشد.

---

## ۷. سیستم بازخورد صوتی و لمسی (Audio-Visual Haptics)

سامانه بوشیدو از سینتی‌سایزر داخلی WebAudio جهت ارتقای تعهد ذهنی استفاده می‌کند:
* `soundFX.playCheck()`: صدای کلیک دقیق و رزونانس برای تیک‌زدن عادات روزانه.
* `soundFX.playStandardDay()`: آکورد پیروزی سبز برای تکمیل روز استاندارد (۸ از ۱۰).
* `soundFX.playMastery()`: آکورد شکوهمند طلایی همراه با بارش کانفتی سامورایی برای کمال تعهد (۱۰ از ۱۰).
* `soundFX.playWarning()`: تون اخطار بم برای روزهای آینده یا تلاش برای تغییر در چرخه قفل‌شده.
* `soundFX.playAutopsySave()`: نوای آرامش‌بخش تسویه بدهی و باز شدن قفل رفتاری.

---

## ۸. راهنمای عملیاتی توکن‌های سیستم (Token Reference Quick Sheet)

برای توسعه سریع و بدون خطا، از این مقادیر و توکن‌های استاندارد استفاده کنید:

```typescript
// 1. Color Surface Hierarchy
Canvas_Root:     '#09090b'  // Tailwind: bg-[#09090b]
Elevated_Card:   '#121215'  // Tailwind: bg-[#121215]
Subtle_Border:   '#27272a'  // Tailwind: border-zinc-800
Hover_Border:    '#3f3f46'  // Tailwind: border-zinc-700
Sub_Container:   '#09090b/70' // Tailwind: bg-[#09090b]/70 (داخل کارت‌ها)

// 2. Text Luminance & APCA Scale
Heading_Primary: '#f4f4f5'  // Tailwind: text-zinc-100 (Lc = 104)
Body_Secondary:  '#a1a1aa'  // Tailwind: text-zinc-400 (Lc = 78)
Muted_Captions:  '#71717a'  // Tailwind: text-zinc-500

// 3. Corner Radius Harmony (Nested Radius Rule: Inner = Outer - Padding)
Modal_Outer:     'rounded-3xl' // 24px
Card_Outer:      'rounded-3xl' // 24px
Inner_Box:       'rounded-2xl' // 16px (24px - 8px padding)
Button_Standard: 'rounded-xl'  // 12px
Tag_Badge:       'rounded-lg'  // 8px or rounded-full

// 4. Heights & Vertical Rhythm
TopBar_Controls: 'h-8' (Mobile) | 'h-9' (Desktop)
Calendar_Nav:    'h-10'
Action_Buttons:  'py-2.5 px-5' (2x Horizontal/Vertical ratio)
```

---

## ۹. چک‌لیست توسعه‌دهندگان برای طراحی کامپوننت‌های جدید

هنگام افزودن هر کامپوننت، ویو یا دکمه جدید به سیستم، این چک‌لیست الزامی است:
- [ ] آیا تمام اعداد با `toPersianDigits` فارسی شده‌اند؟
- [ ] آیا سایز آیکون و محفظه از جدول ۴ سطحی تبعیت می‌کند؟
- [ ] آیا برچسب دکمه‌ها و چیپ‌ها دارای `whitespace-nowrap` هستند؟
- [ ] آیا عنصر تعاملی دارای `cursor-pointer` و کارت اطلاعاتی دارای `cursor-default` است؟
- [ ] آیا در حالت موبایل (عرض ۳۶۰ پیکسل) چیدمان عمودی و بدون شکست نامناسب است؟
- [ ] آیا رنگ‌ها با توکن‌های معنایی (Amber, Emerald, Crimson, Frost, Slate) همخوانی دارند؟

---

## ۱۰. ماژول‌های تحلیلی اتاق فرماندهی (Cycle Analytics Architecture)

1. **ماتریس وفاداری به ارکان دیسیپلین (`HabitFidelityMatrix`):**
   - تحلیل تفکیکی درصد وفاداری و اجرای هر یک از ۵ پایه فونداسیون به صورت جداگانه در طول چرخه ۹۰ روزه.
   - محاسبه نرخ ماموریت شتاب‌دهنده ویژه روزانه (ارتقا از ۸ به ۱۰).
   - دسته‌بندی خودکار ارکان به سطوح آهنین (≥۸۵٪)، استاندارد (۷۰-۸۴٪) و آسیب‌پذیر (<۷۰٪).
2. **ماتریس جامع ۹۰ روزه (`TacticalHeatmap90`):**
   - نمایش کل چرخه ۹۰ روزه در یک کادر یکپارچه بدون خرد کردن‌های غیرضروری و پیچیده.
   - **روز جاری در حال نبرد (In Battle / Active):** تا زمانی که روز جاری به پایان نرسیده یا به حدنصاب استاندارد (۸ یا ۱۰) نرسیده، به صورت خنثی با توکن خاکستری تیره فعال (`bg-zinc-800 border-zinc-600 ring-2 ring-amber-400 text-zinc-100`) نمایش داده می‌شود تا قبل از فرارسیدن کات‌آف فردا، هرگز شکست تلقی نگردد.
   - **تفکیک شفاف پاداش شناختی:** روزهای کمال ۱۰/۱۰ با گرادیان کهربایی/طلایی (`amber-400 to amber-500`) و روزهای استاندارد ۵ پایه‌ای با سبز زمردی (`emerald-500`).
   - کدگذاری رنگی وضعیت هر روز (کمال ۱۰/۱۰، استاندارد ۸/۱۰، امروز در نبرد، فریز، بدهی باز، کالبدشکافی شده، آینده).
   - امکان پرش تعاملی به هر روز در میدان نبرد.

